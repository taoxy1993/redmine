<h2>在线编译</h2>
<big>
<p>
不要刷新本页!<br />
在结果未出来前，不要关闭本页! <br />
请用打开新窗口或标签的方式访问下面的链接!<br /><br />
<p><%= link_to '查看打印',"#{@logpath}",:target=> "_blank" %></p>
<p><%= link_to '查看生成文件',"#{@outpath}" ,:popup => true %></p>
You can use wget to download this directory:<br />
wget -m -np 
<%= @outpath %><br /><br />
<p><%=link_to  "取消编译", {:controller => 'compile', :action => 'canclecompile', :id => @project,:outpath => @e}, :confirm => "您确定是否取消编译?", :method => :post %></p>
<%= link_to '确定', { :controller => 'compile', :action => 'index', :id => @project }%>
</p>

</big>
